import { Typography } from "@mui/material";

export default function HashTag() {
    return (
        <Typography variant="inherit" sx={{ color: "lightblue", display: "inline" }}>
            #
        </Typography>
    );
}
